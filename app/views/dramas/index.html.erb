<div class="row">
	<div class="col s12">
		<% if notice.present? %>
			<p id="notice" class="chip animated rubberBand"><%= notice %><i class="material-icons">close</i></p>
		<% end %>
			<!-- <div id="summary">
					<%= render 'summary_card' %>
			</div> -->
			<div id="dramas" class="transitions-enabled">
				<%= render 'drama_card' %>
			</div>
		<%= render 'floating_btns' %>
	</div>
</div>
