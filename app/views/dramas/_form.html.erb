<%= form_for(@drama) do |f| %>
  <% if @drama.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@drama.errors.count, "error") %> prohibited this drama from being saved:</h2>

      <ul>
      <% @drama.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
